# markdown-it-named-headings

> Add IDs to heading elements in markdown-it

A plugin for `markdown-it` to add `id` attributes to *h1...h6* elements.

```js
md.render('# Example Header')
=> '<h1 id="Example%20Header">Example Header</h1>'
```
